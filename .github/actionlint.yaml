# release.yml is generated by dist (cargo-dist) — its shell style is not ours to
# maintain. Our own workflows are linted strictly; the generated one is skipped.
paths:
  .github/workflows/release.yml:
    ignore:
      - ".*"
